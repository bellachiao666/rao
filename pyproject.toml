[project]
name = "recursive-agent-harness"
version = "0.1.0"
description = "RAO-style recursive agent inference harness"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2",
    "PyYAML>=6",
]
