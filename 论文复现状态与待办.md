# RAO 论文复现状态与待办

最后审查日期：2026-06-10

目标论文：[Recursive Agent Optimization，arXiv:2605.06639v1](https://arxiv.org/abs/2605.06639)

## 一句话结论

**当前代码已完成工程级数据飞轮验证，但尚不能声称复现了论文的 DeepDive 实验效果。**

当前可以运行：

```text
Qwen 0.6B
→ synthetic rollout
→ verifier / reward / advantage
→ LoRA 训练
→ checkpoint 发布
→ 下一轮真实加载新参数
```

当前不能运行：

```text
Qwen-3-4B-Instruct-2507
→ DeepDive 真实搜索和递归 CodeAct rollout
→ LLM node judge
→ AReaL 多卡训练
→ 75 个 optimizer steps
→ held-out 评测
→ 论文基线与消融对比
```

## 当前成熟度

| 等级 | 含义 | 状态 |
|---|---|---|
| L0 | RAO 公式、数据结构和批次编译已有单元测试 | 已完成 |
| L1 | synthetic 模型闭环、多轮训练和断点恢复 | 已完成 |
| L2 | DeepDive 真实短训练 | 未完成 |
| L3 | DeepDive 忠实配置下训练至少 75 steps | 未完成 |
| L4 | 基线、消融和置信区间支持论文效果判断 | 未完成 |

自动检查：

```bash
python3 check_reproduction_readiness.py
```

当前检查结果：

```text
Overall: BLOCKED
Claim level: L1_SYNTHETIC_FLYWHEEL
READY: 3
PARTIAL: 1
BLOCKED: 12
```

## 已经实现

- RAO 本地 node reward。
- root group leave-one-out advantage。
- depth inverse-frequency weighting。
- CISPO 风格 token objective。
- Hugging Face 行为策略 token 和 logprob 采集。
- LoRA 参数更新。
- rollout 数据冻结后再训练。
- adapter、optimizer 和 manifest 原子发布。
- policy version 连续性和 checkpoint hash 校验。
- 多轮训练和进程重启后的 resume。
- 多 GPU rollout 模型副本。
- DeepDive CSV loader 和确定性 split 生成代码。
- DeepDive search、网页读取和 LLM judge 的基础适配器。

这些能力足以证明同步数据飞轮成立，但不足以证明论文效果。

## 必须实现的 P0 项

### 1. DeepDive 真实训练入口

当前 `examples/run_deepdive_rao_training.py` 只执行 preflight，随后主动抛出
`RuntimeError`。

需要实现：

- 从固定 split manifest 加载训练任务；
- 构造 DeepDive 搜索工具；
- 构造 root 和 subtask judge；
- 执行 recursive rollout；
- verifier、reward、advantage 和 batch 编译；
- 调用训练后端；
- 保存并发布 checkpoint；
- 支持失败恢复和 resume。

最低验收：

```text
1 task，G=2，depth=1，完成 1 optimizer step
16 tasks，G=8，depth=4，完成 1 optimizer step
faithful batch 中必须有 16 × 8 = 128 棵 rollout tree
```

### 2. 接通论文使用的 CodeAct 运行时

论文使用的是自然语言思考加 Python REPL，而当前可运行路径使用结构化 JSON
action。`recursive_agent_training/codeact/` 目前只是解析器和 AST 校验原型。

需要实现：

- 模型输出 thought 和 Python 代码；
- 每个 node 拥有持续存在的 REPL 状态；
- Python 中可调用异步 `launch_subagent`；
- 支持顺序子任务和 `asyncio.gather` 并发；
- 支持 `search_web` 和网页读取；
- 子 agent 可返回字符串或结构化对象；
- 精确保存实际生成的 action tokens 和行为 logprobs；
- 限制递归深度、运行时间、网络、输出大小和允许调用。

如果继续使用 existing harness，可以进行实现变体实验，但必须明确标注为
**非忠实 action-space 复现**。

### 3. 搜索和 Judge 工业化

现有适配器缺少长时间实验所需的可靠性控制。

需要实现：

- Tavily 请求和响应标准化；
- 有上限的指数退避重试；
- search 和 judge 独立并发/QPS 限制；
- 搜索结果和网页内容缓存；
- judge prompt、模型和版本固定；
- 严格 JSON schema 校验；
- malformed、timeout 和服务错误的隔离；
- latency、cost、cache hit 和失败率统计；
- policy、judge、search 凭据相互分离。

Judge 失败不能直接当成 reward 0，必须标记为 missing 并禁止进入训练。

### 4. 完整 AReaL 多卡后端

当前 `AReaLOptimizerAdapter` 只是很薄的 optimizer factory 包装。

需要实现：

- Qwen-3-4B 分布式模型初始化；
- rollout worker 生命周期；
- token batch 传输；
- behavior policy version 发布；
- staleness 计算和拒绝；
- current policy logprob 重算；
- 分布式 optimizer step；
- gradient clipping 和有限值检查；
- checkpoint save/load/resume；
- policy snapshot 发布；
- worker 失败恢复。

当前八卡模式是八张卡并行 rollout、GPU 0 单卡 LoRA 训练，不等于八卡分布式训练。

### 5. 40K/256K 长上下文

配置文件中已有 40K train 和 256K eval，但运行时没有真正验证。

需要实现：

- 40,960 token 训练上下文；
- 262,144 token 评测上下文；
- 明确的 prompt/observation 截断规则；
- 保留 system、root task 和近期 observation；
- action token 被截断的样本禁止训练；
- 截断后的 behavior/current logprob 对齐测试；
- 显存和 token 预算预检。

### 6. Held-out 评测执行器

当前 evaluation 代码只聚合已有 JSON，不会执行模型 rollout。

需要实现：

- 固定使用 split manifest 中的 50 个 held-out tasks；
- 每个 task 使用固定数量的 rollout；
- 计算 success rate 和 pass@8；
- 计算固定 seed 的 bootstrap 置信区间；
- 记录 nodes、depth、steps、tokens、delegation、latency 和 cost；
- 记录 tool/judge timeout 和失败；
- 保存逐任务输出。

标准产物：

```text
evaluation_manifest.json
per_task_results.jsonl
summary.json
```

### 7. 论文对比矩阵

必须在同一 held-out split 和相同评测条件下运行：

1. Base model + single agent；
2. RL-trained model + single agent；
3. Base model + recursive agent；
4. RAO-trained model + recursive agent。

还需要执行四组消融：

```text
Dense + Weighted
Dense + Unweighted
Sparse + Weighted
Sparse + Unweighted
```

所有实验必须固定 dataset split、模型初始化、group size、工具、judge、seed 和评测协议。

### 8. 完成 75-step 证据包

论文级实验至少需要：

- 75 个 optimizer steps；
- 训练中断和 resume 验证；
- 周期性 held-out evaluation；
- success 和 pass@8 曲线；
- loss、ratio、gradient norm 和 staleness；
- token、显存、吞吐和 wall-clock；
- search/judge 费用及失败率；
- 四组主要对比；
- 四组消融；
- 置信区间；
- 逐任务结果；
- 与论文实现差异说明。

## 推荐实施顺序

1. DeepDive 同步 LoRA，单任务完成一步训练。
2. DeepDive 同步 LoRA，完成 128-tree faithful batch。
3. 接通 CodeAct REPL 和精确 token/logprob。
4. 实现 held-out evaluator。
5. 实现四组主要对比和消融调度。
6. 接通 AReaL 分布式训练和异步 staleness。
7. 运行 5-step checkpoint/resume 测试。
8. 运行 10-step 短训练并周期评测。
9. 运行完整 75-step 训练和消融。

前两步完成后，只能称为 **DeepDive pipeline validation**，不能称为论文效果复现。

## 最终复现验收标准

只有同时满足以下条件，才建议标记为“论文效果复现完成”：

- 使用并记录 Qwen-3-4B-Instruct-2507 的准确 revision。
- DeepDive 数据 hash 和固定 50-task split 可追溯。
- root batch 16、group size 8、depth 4、每 node 25 steps。
- 学习率 `3e-6`，训练上下文 40K，评测上下文 256K。
- staleness 不超过 3 batches。
- 至少成功完成 75 个 optimizer steps。
- 每个训练样本都能追溯 behavior tokens、logprobs、reward、baseline、advantage 和 weight。
- 四个主要条件使用相同评测协议。
- 报告 success、pass@8、置信区间、失败率、成本和逐任务结果。
- RAO-trained recursive 条件相对 trained single-agent 有可解释的提升。
- 所有不同于论文的实现选择均被明确记录。

仅跑通训练、得到 checkpoint，或者偶然达到某个 headline 数字，都不足以证明复现成功。

## 相关文件

- 英文状态总览：`PAPER_REPRODUCTION_STATUS.md`
- 英文实施清单：`PAPER_REPRODUCTION_BACKLOG.md`
- 机器可读门槛：`reproduction_gates.json`
- 环境变量模板：`reproduction.env.example`
- 自动检查器：`check_reproduction_readiness.py`
