# 代码仓库隐藏问题改进建议

本文档基于当前代码审计结果整理，已按要求忽略第 5 个问题：训练 harness 存在隐藏硬编码上限。

## P0：安全与评测正确性


### 2. 修复 `LLMJudge` 的布尔解析

当前风险：

- `recursive_agent_harness/evaluator.py` 中使用 `success=bool(parsed["success"])`。
- 如果 judge 返回字符串 `"false"`，Python 会将非空字符串判为 `True`。
- 这会污染成功率、reward、准入审计和后续训练。

建议改法：

```python
success = parsed.get("success")
if not isinstance(success, bool):
    raise ValueError("judge JSON field 'success' must be boolean")

reason = parsed.get("reason")
if not isinstance(reason, str) or not reason.strip():
    raise ValueError("judge JSON field 'reason' must be a non-empty string")
```

建议测试：

- `{"success": false}` 被判为失败。
- `{"success": "false"}` 抛出错误。
- 缺少 `success`、`reason` 或类型错误时抛出错误。

### 3. 显式排除失败终态进入训练

当前风险：

- rollout 转换里只要节点状态属于终态集合，就可能被视为完整 tree。
- `assess_node_trainability()` 没有显式排除 `timeout`、`failed`、`cancelled`、`depth_limited`、`node_limited` 等失败终态。
- 如果失败节点带有 token trace 且未被 fallback 标记，可能进入 optimizer batch。

建议改法：

- 在 `assess_node_trainability()` 中加入终态白名单。
- 默认只允许 `terminal_status == "completed"`。
- 如果确实要训练 step limit 样本，仅在配置允许时额外放行 `step_limited`。
- 默认拒绝 `timeout`、`failed`、`cancelled`、`depth_limited`、`node_limited`、`child_limited`。

建议测试：

- completed 且信号完整的节点可以训练。
- timeout/failed/depth_limited 节点即使有 token trace 也不可训练。
- 配置允许时，step_limited fallback 样本的行为符合预期。

## P1：CodeAct 与外部访问边界

### 4. 确保 CodeAct 每个代码块只能产生一个动作

当前风险：

- `CodeActEnvironment.execute()` 最后只检查 `pending_action is not None`。
- 如果模型代码中连续调用 `think()` 和 `finish()`，后一个动作会覆盖前一个动作。
- 训练日志看起来像模型只做了一步，实际中间动作被静默丢弃。

建议改法：

- 在 `CodeActEnvironment` 中增加 `action_call_count`。
- 每个动作函数调用时统一经过 `_set_action()`。
- `_set_action()` 负责递增计数并记录动作。
- `execute()` 结束时要求 `action_call_count == 1`。

建议测试：

- `finish("ok")` 正常返回。
- `think("x"); finish("ok")` 抛出“必须恰好调用一个动作函数”的错误。
- 不调用任何动作函数时继续报错。



### 7. 页面内容截断要显式暴露

当前风险：

- `_open()` 超过 `max_content_bytes` 时会静默截断响应体。
- 模型可能把半页内容当成完整证据，影响答案质量和评测。

建议改法：

- `_open()` 返回结构化结果，例如 `{body, truncated, bytes_read}`。
- 工具 observation 中加入 `truncated: true/false`。
- prompt 中提示模型：如果页面被截断，应披露限制或读取更精确来源。

建议测试：

- 小页面 `truncated=false`。
- 超限页面 `truncated=true`。
- observation 中包含实际读取字节数。

## P2：供应链与工程整理

### 8. 收紧 checkpoint 加载可信边界

当前风险：

- `recursive_agent_training/checkpoints.py` 使用 `torch.load(..., weights_only=False)`。
- 加载不可信 checkpoint 时存在反序列化执行风险。

建议改法：

- 如果 checkpoint 只来自本训练流程，函数名或文档明确标注“仅加载可信 checkpoint”。
- 优先保存和加载纯 state dict。
- 能使用 `weights_only=True` 的场景改为默认启用。
- 对外部模型权重优先使用 safetensors。

建议测试：

- 自生成 checkpoint 可正常保存和加载。
- 非 dict 或缺少必要字段的 checkpoint 被拒绝。
- 文档中明确禁止加载不可信路径。

### 9. 修正文档和依赖分层

当前风险：

- README 提到 `requirements-train.txt`，但仓库中没有该文件。
- 当前 `requirements.txt` 已包含 torch、transformers、peft、modelscope 等重依赖。
- 这与 README 中“inference-only install 不加重 ML 依赖”的描述冲突。

建议改法：

- 将 `requirements.txt` 缩回核心依赖：

```text
pydantic>=2
PyYAML>=6
```

- 新增 `requirements-train.txt`：

```text
torch>=2.4,<2.5
transformers>=4.51,<5
accelerate>=1,<2
peft>=0.15,<0.20
modelscope>=1.37,<2
```

- README 中统一推荐：

```bash
python3 -m pip install -r requirements.txt
python3 -m pip install -r requirements-train.txt
```

或使用：

```bash
python3 -m pip install ".[train]"
```

建议测试：

- 只安装核心依赖时，`recursive_agent_harness` 可 import。
- 安装训练依赖后，训练相关模块可 import。
- README 中的安装命令实际存在且可执行。

