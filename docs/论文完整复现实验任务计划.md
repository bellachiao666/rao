# RAO 论文完整复现实验任务计划

> 根据根目录 `论文复现状态与待办.md` 整理  
> 计划基线日期：2026-06-10  
> 目标论文：Recursive Agent Optimization（arXiv:2605.06639v1）  
> 复现口径：基于现有硬件的缩小模型方法复现，不宣称 4B 原配置数值复现

## 0. 快速确认区

先确认或直接修改下表，确认后再按阶段执行。

| 项目 | 当前计划值 | 确认/修改 |
|---|---|---|
| 主训练模型 | ModelScope `Qwen/Qwen2.5-3B-Instruct@master` | [x] |
| 模型选择门槛 | 可信非零优势、无 CUDA/OOM、峰值显存 <21 GiB、可恢复 | [x] |
| 模型来源 | policy、Judge 及其他模型均只从 ModelScope 拉取 | [x] |
| 数据集 | DeepDive，SHA-256 `126047...2aa` | [x] |
| Held-out 集合 | 固定 50 tasks，split hash `39fe75...c3e` | [x] |
| 主训练配置 | root batch=4，G=4，depth=2，每 node 8 steps | [x] |
| 优化配置 | LoRA；学习率 `1e-4` 短跑校准；主训练 75 steps | [x] |
| 上下文 | 主配置 train=8,192，eval=16,384 tokens | [x] |
| 最大 staleness | 同步训练为 0；异步实验不得超过 3 batches | [x] |
| 主要指标 | success rate、pass@8、bootstrap 置信区间 | [x] |
| 训练随机种子 | 42 | [x] |
| 评测随机种子 | 42 | [x] |
| Judge 模型与版本 | ModelScope `Qwen/Qwen3-1.7B` 本地独立副本 | [x] |
| Search provider 与版本 | DuckDuckGo HTML，无模型、无密钥 | [x] |
| 硬件预算 | 8×RTX 4090 24GB，主机内存 503GB | [x] |
| 服务预算 | DuckDuckGo Search + 本地 Judge，无 API 费用 | [x] |

### 当前执行状态

截至 2026-06-19：

- [x] 本地最终代码状态测试：`144 passed`；远端目标测试与最终报告审计通过。
- [x] ModelScope SDK `1.37.1` 已安装。
- [x] 数据 hash、固定 50-task split 和模型 snapshot hash 已验证。
- [x] 0.6B 与 1.7B S0 preflight 均已通过。
- [x] 0.6B 正常异步运行显存峰值：rollout 约 1.7 GiB/卡，trainer 约 8.2 GiB。
- [x] 1.7B 初始显存压测：rollout 约 4.5 GiB/卡，远低于 24 GiB。
- [x] 0.6B 与 1.7B 各完成 8 次可信 S0 采集，均因无可信非零优势被淘汰。
- [x] ModelScope `Qwen/Qwen2.5-3B-Instruct@master` 已下载并冻结，snapshot hash
  `6e7ce1ee2dc4f72be0d99d7f430739dd6b579639041923837940c5132a7bfa54`。
- [x] 已发现并拒绝两类 Judge 伪奖励：复述 delegated subtask、无事实的元话术。
- [x] 同一 round 的零优势重试已改为复用 policy/Judge/tools，不再反复重载模型。
- [x] 3B 可信 S0 v15 已通过独立 admission audit；历史 v3-v14 均保留为否决证据。
- [x] 远端 SSH、8 卡 GPU 和 `/datadisk8t` 存储均已恢复并验证。
- [x] Search 联网、缓存、重试、限流和指标已验证。
- [x] 本地 ModelScope Judge 已接通，并加入词法假阳性护栏。
- [x] 受限 CodeAct 持久状态、单/并行委托映射、工具和 token/logprob 路径已实现。
- [x] Held-out evaluator、bootstrap CI 和逐任务 JSONL 已实现。
- [x] 四组主要对比和四组消融调度器已实现。
- [x] Search/Judge canary、最终失败指标和缺失 Judge 信号阻断已实现。
- [x] 源码树 hash 和每 10 steps 周期评测调度已实现。
- [x] S0 checkpoint 已在新进程恢复到 `policy_000002`，adapter/optimizer hash 连续。
- [x] CodeAct S0 与 S1 16-tree 单步均通过可信奖励和非零 advantage 审计。
- [x] 5-step checkpoint/resume 与两组 10-step 学习率预实验已完成。
- [x] 相同 10×4 held-out：base recursive 0/40，`1e-4` 训练后 1/40，`3e-5` 训练后 0/40。
- [x] `final-matrix-v1` 五个训练条件均完成 75 steps，均发布 `policy_000075`。
- [x] M1-M4 与 A1-A4 均在同一 50-task held-out split 上完成 8 rollouts/task。
- [x] 最终证据包已生成：
  `/datadisk8t/yongxinxiao/rao-reproduction/final-matrix-v1/report/final_report.md`。
- [x] 报告构建器 `--require-75` 复核通过：5 个训练条件、375 个训练 round、
  8 个周期评测。
- [x] 最终主比较未观察到 RAO recursive 相对 trained single-agent 的正向方法趋势：
  M4-M2 success delta = `-0.0250`，95% paired bootstrap CI
  `[-0.0525, -0.0025]`；pass@8 delta = `0.0000`，
  95% CI `[-0.1000, 0.1000]`。

### 已确认硬件与资源分配

远程服务器于 2026-06-10 实测：

| 资源 | 实测值 |
|---|---|
| GPU | 8×NVIDIA GeForce RTX 4090 |
| 单卡显存 | 24,564 MiB |
| 主机内存 | 503 GiB |
| `/datadisk8t` 剩余空间 | 约 2.2 TiB |
| 已有模型 | ModelScope `Qwen/Qwen3-0.6B`、`Qwen/Qwen3-1.7B`、`Qwen/Qwen2.5-3B-Instruct` |

默认 GPU 分配：

| GPU | 用途 |
|---|---|
| GPU 0 | 预留给其他作业或故障备用 |
| GPU 1-4 | 正式实验 rollout worker |
| GPU 5 | LoRA 训练、optimizer、checkpoint |
| GPU 6 | 备用 rollout/压测 |
| GPU 7 | Qwen3-1.7B 本地 Judge |

如果 Judge 使用独立服务，GPU 7 可加入 rollout；如果训练实测需要两张卡，则从
rollout worker 中释放一张卡，不允许通过把单卡显存跑满来换吞吐。

### 分级实验配置

| 配置 | 模型 | root batch / G | depth / node steps | train / eval context | 用途 |
|---|---|---|---|---|---|
| S0-A 冒烟 | Qwen3-0.6B | 1 / 4 | 1 / 8 | 4K / 4K | 最低成本基线与链路验证 |
| S0-B 准入 | Qwen3-1.7B | 1 / 4 | 1 / 8 | 4K / 4K | 仅在 0.6B 无可信信号时尝试 |
| S0-C 准入 | Qwen2.5-3B-Instruct | 1 / 4 | 1 / 8 | 4K / 4K | 0.6B/1.7B 无可信信号后的候选 |
| S1 主实验 | 准入通过的最小模型 | 4 / 4 | 2 / 8 | 8K / 16K | 75-step、对比、消融 |

主模型准入门槛：

- [x] 单个 rollout worker 峰值显存不超过 20 GiB。
- [x] 单卡 LoRA 训练峰值显存不超过 21 GiB；S1 实测峰值约 11.5 GiB。
- [x] 已完成两组 10 steps，无 CUDA/OOM/NaN/Inf。
- [x] 非零优势来自可核验的精确论文标题，不接受复述或元话术。
- [x] checkpoint 能在新进程恢复并继续发布下一 policy version。
- [x] 不减少最终 50-task held-out 评测覆盖。

上下文升级按 `8K/16K -> 16K/32K -> 40K/256K` 逐级压测。S1 的强制门槛是
8K/16K；40K/256K 仅作为论文配置差异和可选压力测试，不阻塞缩小模型方法复现。

### ModelScope 统一拉取规则

- [x] policy 主模型固定为
  [Qwen/Qwen3-0.6B](https://modelscope.cn/models/Qwen/Qwen3-0.6B)。
- [x] 第二候选模型固定为
  [Qwen/Qwen3-1.7B](https://modelscope.cn/models/Qwen/Qwen3-1.7B)。
- [x] 第三候选模型固定为
  [Qwen/Qwen2.5-3B-Instruct](https://modelscope.cn/models/Qwen/Qwen2.5-3B-Instruct)。
- [x] policy、Judge 及当前实验的所有模型依赖均从 ModelScope 拉取。
- [x] 禁止同一实验混用 Hugging Face 与 ModelScope 的同名模型快照。
- [x] 使用 ModelScope SDK/CLI 固定快照，实验运行时使用实际本地路径。
- [x] 每个模型记录 `model_id`、revision、ModelScope 版本和逐文件/snapshot hash。
- [x] 下载完成后切换到本地离线加载，避免运行中远端 revision 漂移。

## 1. 目标与完成定义

最终目标不是只跑通训练，而是在缩小模型和缩短上下文条件下，形成可审计的
RAO 方法复现证据。

只有同时满足以下条件，才将执行状态标记为“硬件适配完整实验完成”；
方法趋势是否复现由 P9 的 M4-M2 比较单独判定。

- [x] 所有模型来自 ModelScope，并准确记录 model ID、revision、hash 和初始化 checkpoint。
- [x] 记录 DeepDive 数据 hash 和固定 train/eval split manifest。
- [x] 忠实执行论文 action space：thought + Python REPL + recursive subagent。
- [x] 完成至少 75 个 optimizer steps，并验证中断恢复。
- [x] 每个训练样本均可追溯 tokens、logprobs、reward、baseline、advantage 和 weight。
- [x] 在相同协议下完成四组主要条件与四组消融。
- [x] 报告 success、pass@8、置信区间、失败率、成本和逐任务结果。
- [x] 明确记录最终入选模型、8K/16K、缩小 batch/depth/steps 等与论文不一致的部分。
- [x] 结论不使用“Qwen 4B 原配置效果复现完成”的表述。

状态标记统一使用：

- `未开始`
- `进行中`
- `受阻`
- `待验收`
- `已完成`

## 2. 当前基线

| 能力层级 | 当前状态 | 本计划目标 |
|---|---|---|
| L0：RAO 公式、数据结构、批次编译 | 已完成 | 保持回归通过 |
| L1：synthetic 数据飞轮与恢复 | 已完成 | 作为集成基础 |
| L2：DeepDive 真实短训练 | 已完成 | 保留审计证据 |
| L3：硬件适配配置 75-step 训练 | 已完成 | 阶段 8 完成 |
| L4：基线、消融、置信区间 | 已完成 | 阶段 9 完成 |

当前结论：已完成 small-model DeepDive pipeline validation、五组 75-step 训练、
M1-M4/A1-A4 统一 50×8 held-out 评测与最终证据包。最终主比较未支持
RAO recursive 相对 trained single-agent 的正向方法趋势，因此只能声称
“硬件适配完整实验已完成”，不能声称“方法趋势复现成功”或“Qwen 4B 原配置效果复现”。

## 3. 阶段总览

| 阶段 | 目标 | 前置依赖 | 完成门槛 | 状态 |
|---|---|---|---|---|
| P0 | 冻结 ModelScope 模型、协议与环境 | 无 | 所有 manifest 草案确认 | 已完成 |
| P1 | DeepDive 单任务真实训练 | P0 | 1 task、G=4、depth=1、可信 1 step | 已完成 |
| P2 | 硬件适配单批训练 | P1 | 4×4=16 trees、1 step | 已完成 |
| P3 | CodeAct 忠实 action space | P1 | REPL、递归、token/logprob 可审计 | 已完成 |
| P4 | Search/Judge 可靠性 | P1 | 失败隔离、缓存、限流、统计完成 | 已完成 |
| P5 | Held-out 评测与实验矩阵 | P2-P4 | 50 tasks、pass@8、CI 可运行 | 已完成 |
| P6 | 8卡资源编排与模型/上下文准入 | P2-P4 | 主模型选定，S1 8K/16K 稳定 | 已完成 |
| P7 | 5-step/10-step 预实验 | P5-P6 | resume 和周期评测通过 | 已完成 |
| P8 | 入选小模型 75-step 主实验 | P7 | 主训练完整、证据齐全 | 已完成 |
| P9 | 对比、消融与最终报告 | P8 | 8 组实验、CI、差异说明齐全 | 已完成 |

## 4. 分阶段任务

### P0：冻结 ModelScope 模型、复现协议与环境

目标：在消耗训练资源前固定所有会影响可比性的输入。

- [x] `P0-01` 从 ModelScope 固定 `Qwen/Qwen3-0.6B` 及 tokenizer revision。
- [x] `P0-02` 记录 ModelScope model ID、revision、文件 hash、依赖版本、CUDA/驱动和 GPU。
- [x] `P0-03` 确认 Judge 及其他模型也只使用 ModelScope 快照。
- [x] `P0-04` 验证模型可在断网/离线模式下从本地路径加载。
- [x] `P0-05` 固定 DeepDive 原始数据版本并记录 hash。
- [x] `P0-06` 生成并审核 train/eval split manifest，eval 必须为固定 50 tasks。
- [x] `P0-07` 固定训练、rollout、评测和 bootstrap seeds。
- [x] `P0-08` 固定 policy、Judge、Search 的模型/服务版本和凭据边界。
- [x] `P0-09` 冻结 S0/S1 配置、GPU 分配、显存余量和资源预算。
- [x] `P0-10` 建立实验命名规则、产物目录结构和 manifest schema。
- [x] `P0-11` 运行 readiness audit 并保存初始结果。

验收产物：

- [x] `environment_manifest`
- [x] `model_manifest`
- [x] `modelscope_download_manifest`
- [x] `dataset_manifest`
- [x] `split_manifest`
- [x] `experiment_protocol`
- [x] 初始 readiness 报告

决策门：

- [x] 数据、模型、Judge、Search 和预算均已确定。
- [x] 所有模型均能从 ModelScope 固定快照离线加载。
- [x] 后续不同实验禁止临时改变 split 或评测协议。

### P1：DeepDive 单任务真实训练闭环

目标：将 DeepDive 接入真实 `collect -> verify -> compile -> optimize -> publish`。

- [x] `P1-01` 从固定 split manifest 加载训练 task。
- [x] `P1-02` 构造 DeepDive Search、网页读取、root Judge 和 subtask Judge。
- [x] `P1-03` 执行一次 recursive rollout。
- [x] `P1-04` 完成 verifier、node reward、LOO advantage 和 depth weight。
- [x] `P1-05` 编译训练 batch 并调用训练后端。
- [x] `P1-06` 仅在真实非零优势通过证据审查后发布有效 checkpoint。
- [x] `P1-07` 验证进程重启后的 resume。
- [x] `P1-08` 保存完整 trace 和失败日志。

验收场景：

```text
model = 通过准入的最小 ModelScope 候选
1 task
group size = 4
max depth = 1
max steps per node = 8
train/eval context = 4K/4K
完成 1 optimizer step
checkpoint 可在新进程中加载
```

通过标准：

- [x] rollout tree 数量、reward 和训练样本数量一致。
- [x] Judge 失败不会被转换为 reward 0。
- [x] checkpoint manifest 和实际参数 hash 一致。

### P2：硬件适配单批训练

目标：先证明 S1 主配置的一个 batch 可被完整生成、训练和审计。

- [x] `P2-01` 使用入选模型、root batch=4、G=4、depth=2、每 node 8 steps。
- [x] `P2-02` 生成完整 16 棵 rollout trees。
- [x] `P2-03` 禁止接受缺 tree、缺 group 或 partial group。
- [x] `P2-04` 校验每棵 tree 的 node、depth、step 和终止原因。
- [x] `P2-05` 校验每个样本的 behavior tokens 与 logprobs。
- [x] `P2-06` 校验 reward、group baseline、advantage 和 depth weight。
- [x] `P2-07` 完成 1 optimizer step 并发布 `policy_000001`。
- [x] `P2-08` 汇总 token、耗时、吞吐、显存、Search/Judge 成本和失败率。

通过标准：

```text
4 tasks × 4 rollouts = 16 complete trees
train/eval context = 8K/16K
所有训练样本均可追溯
完成 1 optimizer step
```

完成本阶段后，只能标记为 `small-model DeepDive pipeline validation`。

### P3：硬件适配 CodeAct action space

目标：接通论文使用的自然语言思考 + Python REPL，而非结构化 JSON action。

- [x] `P3-01` policy 输出 thought 和 Python code。
- [x] `P3-02` 每个 node 拥有持续存在的受限执行状态。
- [x] `P3-03` Python 中可调用 `launch_subagent`。
- [x] `P3-04` 支持顺序子任务与 `asyncio.gather` 并行委托映射。
- [x] `P3-05` 支持 `search_web` 和网页读取。
- [x] `P3-06` subagent 结果通过 observation 返回。
- [x] `P3-07` 精确保留实际 sampled action tokens 和 behavior logprobs。
- [x] `P3-08` 训练 trace 转换不改变 action-token span。
- [x] `P3-09` 限制递归深度、网络目标、代码结构、输出大小和允许调用。
- [x] `P3-10` 覆盖异常、非法 AST 和资源上限测试。

通过标准：

- [x] 同一 node 可跨多次 action 保留 REPL 状态。
- [x] 顺序和并发 subagent 均能正常返回。
- [x] captured token span 可与模型原始输出逐 token 对齐。
- [x] 使用非 CodeAct 路径的实验自动标记为“非忠实 action-space 变体”。

说明：当前实现是受限 Python action runtime，由 harness 执行递归和工具动作；它不等同于
论文中的通用异步 Python 解释器，因此最终报告必须将其列为硬件适配实现差异。

### P4：Search 与 Judge 工业化

目标：避免长时间训练被服务波动污染。

- [x] `P4-01` 标准化 DuckDuckGo HTML 请求、响应和 provenance。
- [x] `P4-02` 实现有上限的指数退避和终止错误状态。
- [x] `P4-03` 分别限制 Search concurrency/QPS 和 Judge 串行访问。
- [x] `P4-04` 缓存搜索结果、URL 内容和必要元数据。
- [x] `P4-05` 固定来自 ModelScope 的 Judge model ID、revision、prompt 和解析 schema。
- [x] `P4-06` 对 malformed、timeout、网络和服务错误分类。
- [x] `P4-07` Judge missing signal 隔离，禁止进入训练和最终评测聚合。
- [x] `P4-08` policy、Judge 使用本地快照，Search 无凭据。
- [x] `P4-09` 记录 latency、cache hit、重试、最终失败和本地 cost=0。
- [x] `P4-10` 加入 Search/Judge canary。

通过标准：

- [x] 服务失败不会静默改变 reward。
- [x] 重试次数、最终状态和费用均可审计。
- [x] 重复请求能命中缓存且不改变实验语义。

### P5：Held-out 评测器与实验矩阵

目标：执行真实 rollout 评测，而不是只聚合已有 JSON。

- [x] `P5-01` 严格加载 split manifest 中的 50 个 held-out tasks。
- [x] `P5-02` 开发阶段支持 10 tasks×4 rollouts，最终阶段支持 50 tasks×8 rollouts。
- [x] `P5-03` 计算 success rate 和 pass@8。
- [x] `P5-04` 使用固定 seed 计算 bootstrap 置信区间。
- [x] `P5-05` 记录 nodes、depth、steps、tokens 和 delegation。
- [x] `P5-06` 记录 latency、Search/Judge 错误和 cost。
- [x] `P5-07` 保存逐 task、逐 rollout 输出。
- [x] `P5-08` 比较前检查 split、Judge、工具、模型 revision 和 bootstrap config。
- [x] `P5-09` 生成四组主要条件的统一调度入口。
- [x] `P5-10` 生成四组消融的统一调度入口。

每次评测的最低产物：

```text
evaluation_manifest.json
per_task_results.jsonl
summary.json
```

主要对比矩阵：

| ID | 模型 | Agent 模式 | 状态 |
|---|---|---|---|
| M1 | 入选 ModelScope base model | Single agent | 已完成：success=1.5%，pass@8=8% |
| M2 | 入选模型 RL-trained | Single agent | 已完成：success=6.75%，pass@8=20% |
| M3 | 入选 ModelScope base model | Recursive agent | 已完成：success=2.0%，pass@8=14% |
| M4 | 入选模型 RAO-trained | Recursive agent | 已完成：success=4.25%，pass@8=20% |

消融矩阵：

| ID | Reward | Depth weighting | 状态 |
|---|---|---|---|
| A1 | Dense | Weighted | 已完成：success=4.25%，pass@8=20% |
| A2 | Dense | Unweighted | 已完成：success=6.5%，pass@8=18% |
| A3 | Sparse | Weighted | 已完成：success=3.25%，pass@8=14% |
| A4 | Sparse | Unweighted | 已完成：success=8.25%，pass@8=20% |

### P6：8 卡资源编排、模型准入与上下文压测

目标：在 8×RTX 4090 24GB 上选择最小可用模型并稳定运行 S1。

- [x] `P6-01` 从 ModelScope 本地快照初始化 0.6B/1.7B/3B policy 候选和 tokenizer。
- [x] `P6-02` 按 GPU 1-4 rollout、GPU 5 训练、GPU 7 Judge 分配资源。
- [x] `P6-03` 同一 round 的采集重试复用 policy/Judge/tools，只在 round 结束释放。
- [x] `P6-04` 发布 behavior policy version。
- [x] `P6-05` 同步 S1 的 staleness 固定为 0；异步试验拒绝 staleness > 3 的样本。
- [x] `P6-06` 在 current policy 下重算 action token logprobs。
- [x] `P6-07` 完成 LoRA optimizer step。
- [x] `P6-08` 执行 gradient clipping 和 finite-value 检查。
- [x] `P6-09` 原子保存、加载和恢复 checkpoint。
- [x] `P6-10` 发布 policy snapshot 并验证 worker 切换。
- [x] `P6-11` 失败/零优势批次隔离，禁止接受 partial groups。
- [x] `P6-12` 验证 8,192-token 训练上下文。
- [x] `P6-13` 验证 16,384-token 评测上下文。
- [ ] `P6-14` 在有显存余量时压测 16K train / 32K eval；本次未执行，不阻塞 8K/16K 缩小配置结论。
- [x] `P6-15` 记录 40K/256K 未采用或压测失败的原因，不将其设为 S1 阻塞项。
- [x] `P6-16` 固定 prompt/observation 截断规则。
- [x] `P6-17` 保留 system、root task 和近期 observations。
- [x] `P6-18` action token 被截断的样本禁止训练。
- [x] `P6-19` 验证截断后 behavior/current logprob 对齐。
- [x] `P6-20` 添加显存和 token 预算 preflight。
- [x] `P6-21` 使用同一套 preflight 测试 ModelScope Qwen3-1.7B。
- [x] `P6-22` 按可信奖励、CUDA 稳定、显存和恢复门槛选定主模型。

通过标准：

- [x] S1 在 8 卡服务器上完成稳定的 rollout、LoRA 训练和 policy 发布。
- [x] 任一 GPU 峰值显存至少保留约 3 GiB 安全余量。
- [x] checkpoint 可在新进程恢复 optimizer、policy version 和 step。
- [x] 8K/16K 是运行时实测配置，不只是配置文件中的声明。
- [x] 主模型选择有可信奖励、显存、吞吐和稳定性数据支持。

### P7：5-step 与 10-step 预实验

目标：在完整 75-step 运行前发现恢复、漂移和成本问题。

#### 5-step checkpoint/resume

- [x] `P7-01` 首进程运行 2 steps 后正常退出。
- [x] `P7-02` 从 checkpoint 恢复至总计 5 steps。
- [x] `P7-03` 校验 optimizer、policy version、hash 和 step 连续性。
- [x] `P7-04` 在新进程中评测恢复后的 checkpoint。
- [x] `P7-05` 检查无重复 batch、无跳 step、无 partial group。

#### 10-step 短训练

- [x] `P7-06` 完成 `1e-4` 与 `3e-5` 两组 10-step 校准。
- [x] `P7-07` 根据 held-out 点估计冻结主实验学习率为 `1e-4`。
- [x] `P7-08` 按固定协议运行 held-out evaluation。
- [x] `P7-09` 检查 loss、ratio、gradient norm 和 staleness。
- [x] `P7-10` 检查 success/pass@4 趋势和 Judge 错误。
- [x] `P7-11` 汇总 token、显存、吞吐、wall-clock 和本地零 API 成本。
- [x] `P7-12` 根据实测结果确认并启动 75-step 预算。

进入主实验前必须满足：

- [x] 无 NaN/Inf；ratio 均值约 1；同步 staleness=0。
- [x] resume 已验证。
- [x] 周期评测可重复执行。
- [x] Judge 最终失败为 0；Search 服务最终失败率低于 5%。

### P8：75-step 主实验

目标：完成准入通过模型的 S1 硬件适配 RAO 主训练。

- [x] `P8-01` 冻结 `final-matrix-v1` config、四任务课程和所有 manifest。
- [x] `P8-02` 已启动 75-step S1 训练，运行中禁止修改配置。
- [x] `P8-03` 每 step 保存 checkpoint 和 policy snapshot。
- [x] `P8-04` 每 10 steps 运行固定 held-out evaluation。
- [x] `P8-05` 记录训练 success 和 pass@8 曲线。
- [x] `P8-06` 记录 loss、ratio、gradient norm 和 staleness。
- [x] `P8-07` 记录 token、显存、吞吐和 wall-clock。
- [x] `P8-08` 记录 Search/Judge latency、费用和失败率。
- [x] `P8-09` 在 step 30/38 外层中断后执行 resume 并保留恢复审计。
- [x] `P8-10` 完成 `policy_000075` 的独立 50×8 评测。

通过标准：

- [x] 至少 75 个有效 optimizer steps。
- [x] 无不可解释的 step 缺口或 batch 污染。
- [x] 全部 checkpoint、评测和监控数据可追溯。
- [x] 所有模型快照均能追溯到 ModelScope model ID 和 revision。

### P9：主要对比、消融与最终报告

目标：判断缩小模型下是否复现 RAO 的方法趋势，而不只判断系统能否运行。

- [x] `P9-01` 在同一 held-out split 下完成 M1-M4。
- [x] `P9-02` 在其他变量固定时完成 A1-A4。
- [x] `P9-03` 为所有结果计算相同方法的置信区间。
- [x] `P9-04` 比较 RAO recursive 与 RL-trained single-agent。
- [x] `P9-05` 报告逐任务结果和失败案例。
- [x] `P9-06` 报告计算、延迟、Search/Judge 成本。
- [x] `P9-07` 报告工具、Judge 和 worker 失败率。
- [x] `P9-08` 汇总模型规模、batch、depth、steps、上下文和后端等实现差异。
- [x] `P9-09` 明确区分“方法趋势复现”“数值接近”和“原配置效果复现”。
- [x] `P9-10` 输出最终可审计证据包。

最终判断：

- [x] M1-M4 使用完全一致的评测协议。
- [x] A1-A4 除消融变量外其他配置一致。
- [x] RAO recursive 与 trained single-agent 的差值附带不确定性；最终未观察到提升。
- [x] 未通过的任务、服务失败和缺失数据没有从报告中删除。
- [x] 最终标题和结论明确写明实际入选模型及硬件适配配置。

## 5. 全程数据与产物要求

每个训练样本至少保留：

- [x] run ID、step、batch ID、task ID、tree ID、node ID。
- [x] ModelScope model ID、revision、tokenizer revision 和本地快照 hash。
- [x] behavior policy version 和 checkpoint hash。
- [x] prompt、observation、实际 action tokens。
- [x] behavior logprobs 和 current-policy logprobs。
- [x] Judge 原始响应、解析结果和版本。
- [x] node reward、root-group baseline、advantage 和 depth weight。
- [x] 截断信息、终止原因、错误状态和重试记录。

完整证据包至少包含：

```text
manifests/
checkpoints/
training_metrics/
evaluations/
per_task_results/
comparison_tables/
ablation_tables/
cost_and_failures/
resume_audit/
implementation_differences.md
final_report.md
```

## 6. 风险与停止条件

出现以下任一情况时，暂停扩大实验规模。本次最终审计结果：

- [x] 未触发：Judge missing 被错误转换为 reward 0。
- [x] 未触发：action tokens 与 behavior logprobs 无法逐 token 对齐。
- [x] 未触发：S1 batch 少于 16 棵完整 trees。
- [x] 未触发：staleness 超过 3 batches 但样本仍进入训练。
- [x] 未触发：action-token span 因截断而不完整。
- [x] 未触发：checkpoint 无法在新进程完整恢复。
- [x] 未触发：不同实验的 split、Judge、工具或 sampling config 不一致。
- [x] 未触发：出现 NaN/Inf、异常 ratio 或无法解释的梯度问题。
- [x] 未触发：Search/Judge 失败率或成本超过预设阈值。
- [x] 未触发：任一 GPU 峰值显存超过 21 GiB，或连续出现 OOM。
- [x] 未触发：实验运行时从未固定 revision 的远端模型重新拉取文件。

## 7. 建议执行顺序

严格按以下顺序推进，避免提前消耗完整实验预算：

1. [x] 从 ModelScope 固定 0.6B、1.7B、3B policy 候选和 Judge 快照。
2. [x] 冻结协议、manifest、seed、GPU 分配和预算。
3. [x] DeepDive S0 单任务完成一个经人工证据审查的有效 step。
4. [x] S1 16-tree batch 完成 1 step。
5. [x] 接通受限 CodeAct runtime 与精确 token/logprob。
6. [x] 完成 Search/Judge 可靠性控制。
7. [x] 完成 held-out evaluator 与实验矩阵调度。
8. [x] 完成 8 卡资源编排和 8K/16K 验证。
9. [x] 用资源复用版 flywheel 完成 3B 准入，选择通过可信奖励门槛的最小模型。
10. [x] 完成 5-step checkpoint/resume。
11. [x] 完成 10-step 短训练和周期评测。
12. [x] 完成入选小模型 75-step 主实验。
13. [x] 完成四组主要对比和四组消融。
14. [x] 汇总置信区间、逐任务结果和最终报告。

### 7.1 远端执行命令

统一在 `/home/yongxinxiao/Documents/rao` 下运行：

```bash
PY=/datadisk8t/yongxinxiao/venvs/rao-sync/bin/python
ROOT=/datadisk8t/yongxinxiao/rao-reproduction
```

服务 canary（包含资源名、子任务复述、模糊元话术三个负例）：

```bash
PYTHONPATH=. $PY examples/run_service_canary.py \
  --config configs/train/deepdive_qwen06b_s0.yaml \
  --output $ROOT/service_canary.json
```

0.6B、1.7B 与 3B 准入使用同一自动编排。当前从已否决候选之后单独执行
3B：

```bash
PYTHONPATH=. $PY examples/run_model_admission.py \
  --candidate-config configs/train/deepdive_qwen25_3b_s0.yaml \
  --output $ROOT/model-admission-v3 \
  --devices cuda:1,cuda:2,cuda:3,cuda:4 \
  --trainer-device cuda:0 \
  --execute
```

不传 `--candidate-config` 时，命令按 0.6B→1.7B→3B 顺序运行。准入成功会生成
`selected_codeact_s0.yaml` 与 `selected_s1.yaml`；无候选通过时命令非零退出。
对入选 S0 目录做跨进程恢复到 2 steps：

```bash
PYTHONPATH=. $PY examples/run_deepdive_rao_training.py \
  --config <入选的-S0-config> \
  --output <入选的-S0-output> \
  --devices cuda:1,cuda:2,cuda:3,cuda:4 --trainer-device cuda:0 \
  --resume --rounds 2
```

CodeAct S0 与 S1 单步：

```bash
PYTHONPATH=. $PY examples/run_deepdive_rao_training.py \
  --config $ROOT/model-admission-v3/selected_codeact_s0.yaml \
  --output $ROOT/model-admission-v3/selected_codeact_s0_training \
  --devices cuda:1,cuda:2,cuda:3,cuda:4 --trainer-device cuda:0 --rounds 1

PYTHONPATH=. $PY examples/run_deepdive_rao_training.py \
  --config $ROOT/model-admission-v3/selected_s1.yaml \
  --output $ROOT/model-admission-v3/selected_training \
  --devices cuda:1,cuda:2,cuda:3,cuda:4 \
  --trainer-device cuda:0 --rounds 1
```

10-step CodeAct 预实验：

```bash
PYTHONPATH=. $PY examples/run_periodic_deepdive_training.py \
  --config $ROOT/model-admission-v3/selected_s1.yaml \
  --action-profile codeact \
  --output $ROOT/selected-codeact-10step \
  --rounds 10 --interval 10 \
  --final-tasks 10 --final-rollouts 4
```

完整矩阵先生成冻结配置，再让 `dense_weighted` 通过周期调度完成 75 steps；
之后矩阵训练会自动复用已完成的该目录，仅补其余条件：

```bash
PYTHONPATH=. $PY examples/run_reproduction_matrix.py \
  --base-config $ROOT/model-admission-v3/selected_s1.yaml \
  --output $ROOT/matrix --action-profile codeact

PYTHONPATH=. $PY examples/run_periodic_deepdive_training.py \
  --config $ROOT/matrix/configs/dense_weighted.yaml \
  --output $ROOT/matrix/periodic/dense_weighted \
  --training-output $ROOT/matrix/training/dense_weighted \
  --rounds 75 --interval 10

PYTHONPATH=. $PY examples/run_reproduction_matrix.py \
  --base-config $ROOT/model-admission-v3/selected_s1.yaml \
  --output $ROOT/matrix --action-profile codeact \
  --rounds 75 --eval-tasks 50 --eval-rollouts 8 \
  --execute-training --execute-evaluation
```

## 8. 执行记录

| 日期 | Task ID | 状态变化 | 产物/结果 | 问题与决策 |
|---|---|---|---|---|
| 2026-06-10 | P0 | 已完成 | ModelScope/model/data/split/environment manifests | 固定 Qwen3-0.6B 与 8×4090 资源方案 |
| 2026-06-10 | P1 初始运行 | 已否决 | 曾发布 `policy_000001` | 0.6B Judge 将 fallback/prompt copy 误判为成功 |
| 2026-06-11 | P3-P5 实现 | 已完成 | `117 passed`，CodeAct/canary/evaluator/matrix/report；后续远端矩阵与报告审计已验证 | 实现已完成，并被最终矩阵复核 |
| 2026-06-11 | Judge 防伪 | 已完成 | 三类负例 canary 均为 0 | 拒绝资源名、subtask 复述、无事实元话术 |
| 2026-06-11 | 0.6B S0 v11 | 已否决 | 工程稳定，显存充足 | 唯一正奖励为无事实元话术，root success=0 |
| 2026-06-11 | 1.7B S0 v1 | 已否决 | 显存约 4.5 GiB/rollout 卡 | 多次模型重载后出现 CUDA indexSelect assert |
| 2026-06-11 | flywheel 资源生命周期 | 已完成 | 同一 round 重试复用 runtime/Judge/tools | 避免反复加载模型；后续远端 375 round 审计通过 |
| 2026-06-11 | checkpoint admission audit | 已完成 | 正奖励与正优势来源自动交叉核验 | audit 失败的 checkpoint 禁止 resume；最终 375 round admission audit 通过 |
| 2026-06-11 | 0.6B/1.7B 正式准入 | 已否决 | 各 8 次采集，工程稳定且无可信非零优势 | 不降低奖励标准，升级到 3B 候选 |
| 2026-06-11 | 3B ModelScope 快照 | 已完成 | `Qwen/Qwen2.5-3B-Instruct@master`，hash `6e7ce1ee...bfa54` | 等待执行 canary 与可信 S0 |
| 2026-06-11 | 3B 准入启动 | 暂停 | 配置、快照与本地 `117 passed` 已完成 | Codex 外部执行额度提示 18:00 后恢复；服务器和 GPU 正常 |
| 2026-06-12 | 3B S0 v15 | 已完成 | 精确答案、可信正奖励 1、非零 advantage 16、峰值约 8.6 GiB | v3-v14 因伪奖励/无信号等原因否决 |
| 2026-06-12 | S0 跨进程恢复 | 已完成 | `policy_000001 -> policy_000002`，adapter/optimizer hash 连续 | GPU 0 被占用时迁移 trainer 到 GPU 5 |
| 2026-06-12 | CodeAct S0 | 已完成 | 可信正奖励 1、非零 advantage 9、峰值约 7.8 GiB | 首次严格解析失败保留为负证据，修复后重跑通过 |
| 2026-06-12 | S1 16-tree 单步 | 已完成 | 16 trees、可信正奖励 1、非零 advantage 4、峰值约 9.2 GiB | Search/Judge 最终失败均为 0 |
| 2026-06-12 | 5-step resume | 已完成 | `policy_000000 -> policy_000005` 连续；一次零优势批次被隔离后重采 | 无重复 step 或 partial group |
| 2026-06-12 | 10-step 学习率校准 | 已完成 | base 0/40；`1e-4` 1/40；`3e-5` 0/40 | 冻结 `1e-4`；点估计非零但 CI 包含 0 |
| 2026-06-15 | final-matrix-v1 dense_weighted | 已完成 | `policy_000075`；50×8 success=4.5%，pass@8=20%，Judge error=0 | success CI 2.0%-7.5%，pass@8 CI 10%-32%；固定课程差异必须披露 |
| 2026-06-15 | sparse proxy 单步 smoke | 已完成 | 16 trees、可信 proxy 正奖励 2、非零 advantage 28、峰值 9.17 GiB | 独立 admission audit 通过；Search/Judge 终止失败均为 0 |
| 2026-06-15 | final-matrix-v1 其余训练条件 | 已完成 | `single_trained`、`dense_unweighted`、`sparse_weighted`、`sparse_unweighted` 均达到 `policy_000075` | 5 个训练条件共 375 个 published round |
| 2026-06-17 | final-matrix-v1 `single_trained`/`dense_unweighted` | 已完成 | 二者均达到 `policy_000075`；`dense_weighted` 此前已完成 | 仍需最终统一评测和报告，不提前声明方法趋势 |
| 2026-06-17 | sparse proxy 防伪修复 | 已完成并同步远端 | fallback/step-limit 节点不再接收 root proxy 正奖励；相关本地全量 `143 passed`，远端目标测试通过 | 保持“无伪奖励”标准，拒绝 fallback 正奖励进入 admission |
| 2026-06-17 | sparse 真实失败样本与吞吐适配 | 已完成 | `train_failed_trajectories=true` 时 step-limit 失败轨迹可作为负样本；`sparse_*` 采样耐心 `max_collection_attempts=32`，并发 `max_concurrent_groups=4` | 不改变奖励、任务、rollout 数和准入；仅减少 sparse 稀疏信号下的重载/等待 |
| 2026-06-17 | `sparse_weighted` 75-step 训练 | 已完成 | 最终达到 `policy_000075`，admission audit 连续通过 | 多次 attempt 无可信正 root 时 advantage 全 0，必须拒绝并重采，不以 0 奖励伪造训练 |
| 2026-06-18 | final-matrix-v1 统一评测 | 已完成 | M1 1.5%/8%，M2 6.75%/20%，M3 2.0%/14%，M4 4.25%/20%；A1 4.25%/20%，A2 6.5%/18%，A3 3.25%/14%，A4 8.25%/20% | 全部为 50 tasks × 8 rollouts；Judge error 均为 0 |
| 2026-06-18 | 最终报告与方法比较 | 已完成 | `final_report.md`、`joined_results.json`、`method_comparison.json`、`resume_audit.json`、`cost_and_failures.json` | M4-M2 success delta = -2.5pp，95% CI [-5.25pp, -0.25pp]；pass@8 持平；方法趋势未支持 |
| 2026-06-19 | 最终报告审计复核 | 已完成 | `build_reproduction_report.py --require-75` 输出 training_conditions=5、training_rows=375、periodic_evaluations=8 | 远端矩阵进程已退出，最终证据包稳定 |

## 9. 待确认问题

- [x] 训练和评测使用一个随机种子：`42`。
- [x] Judge 使用 ModelScope `Qwen/Qwen3-1.7B@master` 本地确定性生成，固定 prompt/schema。
- [x] Search 使用 DuckDuckGo HTML，QPS=1，并发 S0=2/S1=4，无 API 费用。
- [x] 75-step 主训练硬件为 8×RTX 4090 24GB。
- [x] 远程服务器最长连续运行时间和调度限制：无限制。
- [x] 周期评测间隔：每 10 steps。
- [x] Judge 最终失败必须为 0；Search 最终失败率超过 5% 暂停扩大实验；API 成本上限为 0。
- [x] 不预先绑定 0.6B 或 1.7B；以通过准入的最小模型作为主要判据。
- [x] 如果 1.7B 是唯一通过可信奖励与稳定性门槛的候选，可用于完整 75-step。
